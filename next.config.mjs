/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/Protfolio",
  assetPrefix: "/Protfolio/",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
